# エラー情報

```
1. 統計情報を含むエンドポイントの利用:
    - バックエンドが提供する
  /api/v1/projects/active エンドポイントを追加
    - このエンドポイントはプロジェクトリストにタ
  スク統計情報（completedTasks, totalTasks,
  progress）を含めて返す
  2. APIクライアントの強化:
    - getActiveProjects 関数を追加し、タスク統計
  情報を含むプロジェクト一覧を取得
    - フォールバックメカニズムを実装し、新しいAPI
  が使用できない場合は従来のAPIを使用
  3. データ取得ロジックの最適化:
    - usePlans フックの fetchPlans
  関数を修正して新しいエンドポイントを使用
    - 元のデータ構造をそのまま活用できるようにロ
  ジックを調整

  この修正により、バックエンドからプロジェクトと
  タスクの関連データが正しく取得され、ダッシュボ
  ードでのプロジェクト表示が改善されます。また、
  フォールバック処理を実装することで、万一新しいA
  PIが利用できない場合にも対応できるようになりま
  した。

  これでダッシュボードのプロジェクト表示問題が解
  決するはずです。
　といったことがありましたので報告します。次の課題はダッシュボードで統計情報の表示が000であるということです
```

# 関連ファイル

